Para correr el programa se ingresa en consola:
> make
> ./scanner < (name_of_file_to_be_scanned) por ejemplo ./scanner < tests/linuxmain.c

Para correr el preprocesador, se compila con gcc y genera output.c
